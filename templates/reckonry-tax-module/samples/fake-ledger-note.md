# Fake Ledger Data

The tests build synthetic ledger events in memory. This file exists to keep the
template visibly seeded with fake data and to document that real taxpayer data
must not be committed.

